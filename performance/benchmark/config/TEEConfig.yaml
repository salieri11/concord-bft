# prerequisite: Enable TEE

simpleConfig:
  blockchain: concord
  nodes: # committer nodes
    - ip:
      port: 50051
      percentage: 100
    - ip:
      port: 50051
      percentage: 0
    - ip:
      port: 50051
      percentage: 0
    - ip:
      port: 50051
      percentage: 0
  workloads:
    - dapp: TEE
      rateControl: 0 # Throttled RPS
      logging: false
      params:
        - RunTest # TEE operation name (either RunTest or SkvbcWrite)
        - 1000 # Total no. of requests
        - 16000 # Size of request (max 60 KB)
  numberThreads: 1 # No of concurrent requests
  outputDir: ./perf_result
advancedConfig:
  wavefront:
    enabled: false