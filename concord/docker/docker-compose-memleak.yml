version: '3'
services:
  concord1:
    image: "${concord_repo}:${concord_tag}_memleak"
    volumes:
      - /tmp:/tmp
  helen:
    command:
      - java
      - -jar
      - helen-1.0-SNAPSHOT.jar
      - --ConcordAuthorities=concord1:5458
