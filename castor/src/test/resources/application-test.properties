# client min and max count for validation
castor.deployment.num.clients.range={1,10}
# max clients allowed per group
castor.deployment.clients.per.group=6
# max number of groups allowed
castor.deployment.client.groups.max=10